import { StyleSheet } from 'react-native';
import theme from '../../theme';

const formikTextInputStyles = StyleSheet.create({
  errorText: {
    marginTop: 5,
    color: theme.colors.error,
  },
});

export default formikTextInputStyles;
